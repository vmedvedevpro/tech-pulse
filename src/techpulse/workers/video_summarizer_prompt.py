SUMMARIZE_PROMPT = (
    "Summarize the following YouTube video transcript in 2-3 sentences. "
    "Write the summary in the same language as the transcript ({language}). "
    "Focus on the core idea, key claims, and what the viewer will take away. "
    "Output ONLY the summary text — no preamble, no quotes, no commentary.\n\n"
    "Transcript:\n{transcript}"
)
